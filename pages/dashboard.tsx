import React from 'react';

const Dashboard = () => {
  return (
    <div>
      {/* <button type="button" onClick={() => authStore.logOut()}>
        로그아웃
      </button> */}
    </div>
  );
};

export default Dashboard;
